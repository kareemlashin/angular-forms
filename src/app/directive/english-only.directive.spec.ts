import { EnglishOnlyDirective } from './english-only.directive';

describe('EnglishOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new EnglishOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
