import { ArabicOnlyDirective } from './arabic-only.directive';

describe('ArabicOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new ArabicOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
