import { EnglishAndNumberDirective } from './english-and-number.directive';

describe('EnglishAndNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new EnglishAndNumberDirective();
    expect(directive).toBeTruthy();
  });
});
