import { ArabicAndNumberDirective } from './arabic-and-number.directive';

describe('ArabicAndNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ArabicAndNumberDirective();
    expect(directive).toBeTruthy();
  });
});
